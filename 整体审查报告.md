# Loom 整体审查报告

> 审查范围：改造后的 Loom 项目（GLM-5.3-Flash 按《Loom架构演进与改造计划书》落地后的版本）
> 审查依据：《Loom架构演进与改造计划书》、`readme.md`（最终设计书）、`docs/architecture/*`、源码与测试
> 审查基准：`go build ./...` 通过、`go vet ./...` 干净、`go test ./...` 全部 `ok`（约 10700 行 Go + 22k 行前后端）

---

## 一、总体结论

这是一次**完成度极高**的改造。七个阶段（0–6）的核心目标几乎全部落地，而且不是空有外壳——异步任务队列带目标去重、退避重试与崩溃自愈；备份用 `VACUUM INTO` 做原子快照、两阶段恢复、AES-256-GCM 加密；事实/派生/证据指纹分离；`main.go` 从 238 行砍到 ~75 行的薄启动器。代码注释密度和质量都高于一般开源项目，测试覆盖了 handler / service / repository / backup / tasks 各层。

**因此这份报告不是「挑错」，而是「找剩余短板」**。改造方向正确、工程化程度高，下面列出的问题大多属于「精益求精」层面，少数几项（密钥泄露、配置写回、并发相关的边界）值得在继续演进前处理。

---

## 二、需要优先处理的问题（P0 / 安全与正确性）

### 1. `GET /api/config` 把 API 密钥明文回传给浏览器

**位置**：`internal/handler/config_handler.go` 的 `Get`；根因在 `internal/config/config.go`。

```go
// config.go
APIKey     string `yaml:"api_key" json:"api_key"`       // 会序列化
EmbedAPIKey string `yaml:"embed_api_key" json:"embed_api_key"` // 会序列化
AuthToken  string `yaml:"auth_token" json:"-"`          // ✅ 已正确隐藏
Passphrase string `yaml:"passphrase" json:"-"`          // ✅ 已正确隐藏
```

`AuthToken` 和 `Passphrase` 都正确加了 `json:"-"`，但 `APIKey` / `EmbedAPIKey` 没有。`ConfigHandler.Get` 返回 `"config": h.cfg`，于是**真实的 LLM 密钥 / Embedding 密钥以明文出现在 `GET /api/config` 的响应体里**。

而 `PrivacyInfo` 的注释写的是「none of them include the token or keys themselves, only whether they are set」——这句话对 `privacy` 字段成立，但对同响应里的 `config` 字段不成立。这是设计意图（隐私面板只报布尔）与实现（密钥随 config 一起下发）之间的矛盾。

问题有两层：
- 安全面：任何能访问 `GET /api/config` 的进程/脚本（或 CSRF、被注入的依赖）都能直接读到云端密钥。对一个「默认不鉴别」的本地服务来说，密钥等于裸奔在浏览器页面上。
- 契约面：与《计划书》阶段 4「日志中禁止输出……模型密钥」「用户能清楚知道哪些数据会发给模型服务」的隐私目标相冲突。

**建议**：给 `APIKey` / `EmbedAPIKey` 加 `json:"-"`，前端设置页保存时用「只写不回读」的方式（输入框留空表示「未修改，沿用旧值」），或返回 `api_key_set bool` 之类的布尔。让 `GET /api/config` 只暴露「是否已配置密钥」，不暴露密钥本身。

---

### 2. `PUT /api/config` 用 `yaml.Marshal(h.cfg)` 整体重写 config.yaml，有丢配置 + 密钥明文落盘的隐患

**位置**：`internal/handler/config_handler.go` 的 `Update`。

```go
data, err := yaml.Marshal(h.cfg)      // 序列化整个 Config
os.WriteFile(h.configPath, data, 0644) // 0664 权限，且包含 auth_token / passphrase 的 yaml 明文
```

三个问题：
1. **注释会丢失**。当前 `config.yaml` 里每一段都有精心写的中文注释（「数据边界开关」「auto_token 留空表示不开启」等）。一次设置页保存后，这些注释全部被 `yaml.Marshal` 抹掉，用户再也看不到自己为何这么配。
2. **`0644` 权限**。敏感配置（`auth_token`、`passphrase`、`api_key`）会被写成同组/其他人可读。虽说是本地单机，但《计划书》阶段 4 明确要求「增加配置文件权限和敏感配置检查」。至少应 `0600`。
3. **幂等/回环风险**：`Update` 是「替换整个 llm 块」并把 `async_extract`、`allow_remote` 这两个 `config.yaml-only` 开关从旧配置复制过来。逻辑本身是稳的，但如果未来 `Config` 新增只读字段，`yaml.Marshal` 会把内存里的默认值也写进去，造成「表面没改、文件却变了」。

**建议**：改为「增量写回」——只 marshal 需要持久化的字段，或维护一个独立的 `config.yaml[v]` 模板；至少把写文件权限降到 `0600`；并考虑保留注释（用字段级结构化更新，或写回前合并原文件）。

---

### 3. 审计日志只记「方法 + 路径 + 状态码」，无法回答「谁删了谁」

**位置**：`internal/app/middleware.go` 的 `AuditMiddleware` / `AuditEntry`。

当前 `audit_log` 只有 `(action, path, status)`。对 `DELETE /api/events/:id` 这种路径，审计只能告诉你「删除了某个事件、返回 200」，**删了哪条、哪个人物被级联了，全都不可追溯**。

《计划书》阶段 4 要求「对导出、恢复、删除和索引重建增加审计记录」，阶段 6 要求「新功能不会绕过事实/派生物/证据的设计边界」。这里语义上「记了」，但信息量不足以支撑事后定位——尤其对「删除」这类不可逆操作。

**建议**：审计中间件对写操作顺带提取 URL 里的 ID（`:id`、`:personID` 等）或响应体里的实体 id，作为一列 `target_id`；重要操作（导出、恢复、删除、reindex）额外记录一个 `detail` 摘要。最低限度：把带 `:id` 的路径参数解析进审计条目。

---

### 4. `VACUUM INTO` 备份在加密模式会**先落一份明文临时文件**再加密

**位置**：`internal/backup/backup.go` 的 `Create`。

```go
tmp := filepath.Join(s.dir, ".vacuum-<nano>.tmp")   // 明文 SQLite，落在备份目录
db.Exec(`VACUUM INTO ?`, tmp)
...
if s.passphrase != "" {
    encryptFile(tmp, dst, passphrase)              // 读明文 → 写 .enc
    os.Remove(tmp)                                  // 成功后删明文
}
```

功能正确（失败/成功路径都 `defer`/显式清理了明文），但存在一个窗口：**在 `VACUUM INTO` 到 `os.Remove(tmp)` 之间，一份完整的明文数据库躺在磁盘上**，且与 `.enc` 快照同目录。对「备份加密」这一安全承诺而言，这是打折扣的：窃取者只要抢在这个窗口读走 `.vacuum-*.tmp` 即可绕过 AES。

**建议**：加密模式把 `VACUUM INTO` 的临时文件写到系统临时目录（`os.CreateTemp("")`，`0600`）而非备份目录；或加密时用流式写法（当前是 `os.ReadFile` 整块读入内存，大库时也有内存峰值问题）。至少明确文档化这个窗口。

---

## 三、设计与健壮性建议（P1 / 值得改进）

### 5. 任务队列的「失败历史」是纯内存的，进程重启后 `GET /api/tasks` 看不到任何失败记录

`internal/tasks/tasks.go` 的 `Queue` 明确 design 成「内存态 + 数据库行是真相」。这是**正确且克制**的设计（注释也讲得很清楚），但代价是：**重启后，历史上失败过的任务、失败原因、重试次数全部消失**。用户看到某条记录 `extraction_status=failed`，但在 `/api/tasks` 页面看不到「为什么失败 / 试了几次」。

而失败原因其实**持久存在**在 `events.extraction_error` 列里。所以问题不是「数据丢了」，而是「队列视角」与「记录视角」在重启后断裂：一个讲历史、一个只讲现状。

**建议**：`GET /api/tasks` 合并两个来源——内存队列的当前状态 + `events` 表里 `extraction_status` 非空且非 `succeeded` 的记录（带 `extraction_error`、`extracted_at`）。这样「失败原因」永远可查，不必为持久化队列引入新的表。

---

### 6. 异步提取的 `POST /api/events` 返回 201，但前端对「处理中 → 完成/失败」的暴露是轮询而非推送

《计划书》阶段 2 明确要求「前端显示处理中、失败、重试和完成状态」「前端查询状态或接收 SSE 进度」。当前实现里队列状态通过 `GET /api/tasks` 暴露，但前端需要**手动刷新或轮询**才能看到一条新记录从 `pending` 变成 `succeeded/failed`。

`readme.md §5.1` 原文「个人使用，单次请求耗时 3-15 秒可接受。不做异步队列，前端显示 loading 即可」——这句**已经过时**了（当前默认 `async_extract=true`），和阶段 2 的异步化是矛盾的，文档里应同步修订。

**建议**：短期可接受轮询（个人规模），但至少在 `readme` 里把「不做异步队列」改为「默认异步，`async_extract` 可回退同步」；中期如果要更好体验，加一个轻量 SSE/WebSocket 进度通道（计划书里也预留了）。

---

### 7. `maintenance` 的孤儿向量清理是基于「当前 id 集合」的启发式，代价是复杂度的隐性耦合

`internal/service/maintenance.go` 的 `isOrphan` 对 `chunk_type` 做了 `event_summary` / `trait` 的硬编码分支，未知 chunk type 一律「不删」。这是**保守且正确**的（宁可不删，不可误删），但也意味着：**未来新增一种向量 chunk（比如计划的 `advice` chunk）时，清理器会静默漏掉它们的孤儿**——因为「未知类型不删」同时把「该删但没删」也保护了。

**建议**：把 chunk 类型 → 源表的映射收敛到一处（比如 `vecRepo` 里一个显式表驱动结构），maintenance 只消费这份映射。这样加新类型时编译器/表驱动能提醒「清理器也要覆盖它」。

---

### 8. 关系图谱的 `limit` 钳制与 `truncated` 标记是好的，但「组织/边」没有同等的截断保护

`internal/service/graph_service.go` 给**人节点**设了 `DefaultMaxNodes=1000 / HardMaxNodes=5000` 并返回 `Truncated` / `NodesTotal`。但 `edges`（关系边 + 任职边）和 `co_attendance`（共同经历对）是**全量返回**的，分别只对 co_attendance 做了 `ListCoLinks(500)` 硬编码截断。

对于一个「关系图谱」来说，**边的数量通常远大于节点**（每个人可能有多条关系、多段任职、多组共同经历）。个人规模可能暂时无感，但一旦数据上千，`edges` 全量下发会让 `GET /api/graph` 的响应体先于前端过滤爆炸。`ListCoLinks` 的 500 截断与 GraphNode 的 1000/5000 也没统一口径。

**建议**：给边和 co-attendance 也引入与节点一致的上限 + `truncated` 标记，或至少在文档里明确「个人 CRM 量级、边全量返回」的边界，避免「节点有上限、边无上限」的口径不一致。

---

### 9. `ConfigHandler.Update` 的「替换整个 llm 块」是脆的

见 §2-2，但单独强调契约面：`Update` 要求 `endpoint/extract_model/advice_model` 非空，但**其他字段（`max_tokens`、`embed_dim` 等）未做范围校验**。例如 `embed_dim` 被写成 0 或负数，会被 `req.EmbedDim == 0 → 沿用旧值` 兜住，但 `max_tokens: -1` 这类值会一路写进 config.yaml 并可能被后续 `ResolvedMaxTokens` 误判。

**建议**：给 write 路径补一层字段级校验（`embed_dim` 正整数、`max_tokens` 正整等），与 read 路径的 `Resolved*` 兜底对齐。

---

## 四、小问题与一致性（P2 / 锦上添花）

### 10. 文档与实现存在若干迟滞点

- `readme.md §5.1`「不做异步队列，前端显示 loading」已过时（当前默认异步）。
- `readme.md §十三 明确不做的事` 里仍然写着「❌ 不做数据加密」，但实现里**已经有**备份 AES-256-GCM 加密（章节末尾的补充说明意识到了，但列表本身仍自相矛盾——建议把「不做数据加密」改为「不做整库静止加密，备份可加密」）。
- `readme.md §13` 说「AI 处理将从同步改为进程内任务队列（异步化）……正在补齐」，但改造**已经完成**，这行应改为完成态。

### 11. `go.mod` 声明的 Go 版本 vs 实际使用的特性

`readme` 里技术栈写「Go 1.27」，但 `go.mod` 需要核对。`readme` 还提到 `uuid` 标准库、`encoding/json/v2`（Go 1.26+ 的实验特性）。实际代码 `internal/handler` 用的是 `github.com/google/uuid`（第三方）而不是标准库 `uuid`。若 README 声称「用 `uuid` 标准库」，需与代码对齐。

### 12. 测试覆盖仍有缺口（虽整体扎实）

`go test ./...` 全绿，但：
- `internal/config`、`internal/frontend`、`internal/models` 无测试（可接受，多为纯数据结构/嵌入）。
- **并发/竞态**未做 `-race` 验证。`tasks.Queue`、`backup.Service` 都用了 `sync.Mutex`/`atomic.Bool`，但 `ingest.Create` 里「先 `CreatePendingEvent` 再 `queue.Enqueue`」这段在 race 下是否安全，值得 `go test -race ./...` 跑一遍确认（尤其 `AIService` 被多个 goroutine 共享 `cfg` 指针的场景）。

### 13. 少量魔法值散落

- `tasks.go` `queueBuffer = 1024`、`rememberLocked` 里 500 条剪枝、100 条一批删除。
- `ai_service.go` 里硬编码的 `10`（最近事件）、`5`（建议检索 Top-k）、`3`（trait 检索 Top-k）。
- `graph_service.go` 的 `ListCoLinks(500)`、`backup.go` 的 `firstDelay = 15s`。

这些都不是 bug，但作为「配置可调项」集中到 config 或常量区，会让「个人规模」的假设更显式、更好调。

---

## 五、改造计划验收对照（对《计划书》验收总表）

| 验收项 | 状态 | 备注 |
|---|---|---|
| README 与代码/库/页面一致 | ⚠️ 部分 | 见 §10，异步化、加密、Go 版本仍有过时表述 |
| `main.go` 只待启动/关闭/装配 | ✅ | 238→~75 行，装配在 `internal/app` |
| 核心业务按领域模块组织 | ⚠️ 部分 | 仍是按层（handler/service/repository）而非计划书的「按领域模块」，但分层清晰、依赖方向正确，属可接受的单体分层 |
| AI 任务状态/重试/超时/幂等/优雅停机 | ✅ | `internal/tasks` 完整 |
| 事实/派生可区分、可追溯、可失效 | ✅ | `evidence_version` FNV 指纹、`source_stale` 读时算 |
| 数据导出/自动备份/恢复校验/向量重建 | ✅ | `internal/backup` + `/api/ai/reindex` |
| 默认不暴露局域网 | ✅ | 默认 `localhost`，`WarnIfExposed` 告警 |
| 日志不泄露敏感内容/密钥 | ⚠️ 部分 | 日志层做了（rosetta logger 压到 Warn + truncate）；**但 `GET /api/config` 暴露密钥属另一条路径，见 §2-1** |
| API 稳定契约 + 前端完整异步状态 | ✅ | `routes.golden.txt` 金样 + `X-API-Version` + 错误包络 |
| 测试/冒烟/恢复/性能可重复 | ✅ | release/drill 脚本 + 大数据量测试 |
| 发布与迁移流程明确 | ✅ | `docs/RELEASE.md` + `scripts/release.*` |

---

## 六、建议的下一步优先级

1. **（P0，半小时）** `APIKey` / `EmbedAPIKey` 加 `json:"-"`；`GET /api/config` 只回布尔（`api_key_set` / `embed_api_key_set`），前端设置页配「留空=沿用旧值」。
2. **（P0，半小时）** `ConfigHandler.Update` 的 `os.WriteFile` 权限改 `0600`；考虑保留注释的增量写回。
3. **（P1，一小时）** 审计中间件提取路径 `:id` 落 `target_id` 列；对应迁移 `v10`。
4. **（P1，半天）** `GET /api/tasks` 合并 `events.extraction_error`，让失败历史重启后仍可见。
5. **（P1，半天）** 加密备份的明文临时文件改写到系统临时目录（`0600`），消除明文窗口。
6. **（P2）** 跑一遍 `go test -race ./...` 兜底；同步修订 README 的三处过时表述（异步化、加密、Go 版本）；整理散落的魔法值。

---

## 七、一句话总结

改造质量远超「能用」，多数目标闭环且可观测、可回退、可演练。剩下最值得处理的是**「密钥在 `GET /api/config` 明文回传」和「配置整文件重写丢注释/泄权限」这两处安全与运维痛点**，以及若干「文档追平实现」「失败历史持久可见」这类打磨项。核心架构无需再动，把这两三处补上，就是一份可以长期自用的本地产品。