# 闲置功能审计（前端未调用部分）

> 审计日期：2026-09-14　|　基线：当前工作区（关系图谱已整体移除 + 本文件所述清理，改动尚未提交）
>
> 方法：以 `internal/app/router.go` 注册的**全部 65 个** `/api` 路由为清单，逐一与 `web/src` 的实际调用比对。
> 前端所有 HTTP 请求都经过 `web/src/api/client.ts`（`fetch(` 只出现在该文件的两个 helper 里，无绕过路径），
> 因此比对是完备的。`_test.go` 里的引用不计入「前端调用」。
>
> 本文件取代 2026-09-13 首版。首版建议的多数项目**已经执行**：`GET /api/tasks`、
> `GET /api/dashboard/stats`、`POST /api/maintenance/cleanup`、`GET /api/relationships`、
> `GET /api/relationships/co-attendance`、`GET /api/positions/:id`、`GET /api/organizations/:id`
> 现在都已不存在；本轮再叠加关系图谱下线（`GET /api/graph` 与
> `GraphHandler`/`GraphService`/`GraphRepo`/`models/graph.go`）。
>
> ⚠️ **本文档 2026-09-14 二次修订**：修正了首版对 A 组的一处事实错误
> （「关系边可由事件提取自动产生」——不成立），并据此把 A/B 两组处置改为保留。
> 处置结果见第七节。
>
> ✅ **2026-09-14 结案：A/B 两组最终也删了。** 当时的「保留」判断基于「后端接口可用、可用
> curl 手工维护」，但实际使用中这两块数据始终是 0 行，产品定位（记一笔 → 攒画像 → 问建议 →
> 转跟进）里没有它们的容身之处，于是被整体下线：A 组（关系边，5 个端点）与 B 组（结构化
> 任职，5 个端点）的端点、服务、仓储、模型、前端封装、AI prompt 段与报告段全部删除，
> 路由表面 62 → 54。同时把上一轮误删的组织归档/恢复端点**恢复**并补上组织页入口。
> 决策记录见 `docs/architecture/decisions.md` ADR-011，变更明细见 `CHANGELOG.md`。
> 第七节的保留理由从此只作历史存档，不再是当前状态。

## 一、总览

65 个端点中 **26 个（40%）前端零调用**，集中在 7 组。性质分三类：

| 类型 | 组 | 说明 |
|---|---|---|
| 半成品功能 | E | follow-up 状态机后端完整，前端只用了 list + create |
| 安全/运维阀 | F、G | 手动备份恢复、审计读取——没有界面，但存在理由 |
| 只能靠 API 手工维护 | A、B、C、D | 关系边、任职、组织归档、AI 推断，前端一次都没调过 |

其中 **C、D 两组共 3 个端点已删除**，A、B、E、F、G 保留（理由见第七节）。

## 二、本轮已删除（零风险，不涉及任何后端能力）

| 文件 / 符号 | 为什么是死的 |
|---|---|
| `web/src/components/ui/card.tsx` | shadcn 风格 Card 组件，全仓库无人 import（页面用的是 `layout.tsx` 的 `SectionCard`） |
| `web/src/App.css` | 无人 import，样式只走 `index.css` |
| `web/src/assets/hero.png`、`react.svg`、`vite.svg` | 无任何引用；favicon 走 `public/favicon.svg` |
| `EventTimeline.tsx` 的 `EventTimelineDense` | 无调用方（连带的 `cn` 导入一并删除） |
| `EventStatus.tsx` 的 `statusLabel` | 无调用方，`EventStatusBadge` 内部自带映射表 |
| 上一轮：`routes/Relationships.tsx`、`graphApi`、5 个 `Graph*` 类型 | 关系图谱下线（见 CHANGELOG `[未发布]`） |

## 三、零调用端点（按能力分组，待决策）

### A. 关系写接口（4 个）— **决定：保留**

`POST /api/relationships`、`GET /api/relationships/types`、`PUT /api/relationships/:id`、`DELETE /api/relationships/:id`

- 涉及代码：`relationship_handler.go` 的 Create/ListTypes/Update/Delete、
  `relationship_service.go` 同名方法、`relationship_repo.go` 的写路径
- **保留理由（附一处更正）**：本文首版在这里写的「代价：关系边只能由事件提取（ingest）
  自动产生」**是错的**。全文检索 `INSERT INTO person_relationships` 只有
  `RelationshipRepo.Create` 一处，而它在生产代码里的调用方只有两处：本组这 4 个端点，
  以及下方 D 组的 `InferHierarchy`。事件提取链路根本不写关系边。
  也就是说删掉本组 + D 组之后，这张表在代码里将**彻底没有任何写入路径**。
  用户上一轮明确说过「手工维护走 API」，前端零调用不能证明没在用 curl 维护 → 保留。

### B. 任职写接口（4 个）— **决定：保留**

`POST /api/persons/:id/positions`、`PUT /api/positions/:id`、`DELETE /api/positions/:id`、`GET /api/organizations/:id/members`

- 涉及代码：`position_handler.go` 的 Create/Update/Delete/ListByOrg、
  `position_service.go`、`position_repo.go` 写路径
- **保留理由**：同上。`INSERT INTO person_org_positions` 只有 `PositionRepo.Create`
  一处，生产调用方只有 `POST /api/persons/:id/positions` 与已删除的 infer-hierarchy。
  记忆里「任职段可以靠 AI 推断产生」同样不成立——D 组就是那个 AI 推断。
- 注意 `GET /api/persons/:id/positions` 只被「问一问」页读（显示现任组织），本来就要留

### C. 组织归档（2 个）— **决定：删除（已执行）**

`POST /api/organizations/:id/archive`、`POST /api/organizations/:id/restore`

- 已删除：`organization_handler.go` 的 Archive/Restore、`organization_service.go` 与
  `organization_repo.go` 的对应方法、`organization_handler_test.go` 的
  `TestOrganizationArchiveHidesFromPickers`
- **代价（已实现）**：组织只能删除，不能停用保历史。`archived_at` 字段、
  `GET /api/organizations?include_archived=true` 读取、前端 Organizations 页的
  「已归档」删除线与 `activeOrgs` 过滤**都保留**——存量已归档组织仍然看得见，
  只是再没有任何地方能产生新的归档。这一层残留是刻意的：清掉它会让存量数据从界面上消失。
- ⚠️ 与 A/B 的取舍标准其实同源：如果「curl 手工维护」是保留 A/B 的理由，那归档能力
  同样只有 API 能碰。真要反悔，`git restore` 这一组 4 个文件即可。

### D. AI 推断上下级（1 个）— **决定：删除（已执行）**

`POST /api/ai/infer-hierarchy`

- 已删除：`ai_handler.go` 的 InferHierarchy、`ai_service.go` 的 InferHierarchy 与
  `hierarchyPair`/`hierarchyPrompt`/`activeEdgeExists`、前端 `aiApi.inferHierarchy`
- 理由：唯一入口（关系图谱页的「AI 推断上下级」按钮）已在上一轮下线，删图谱页时就已成孤儿。
  它同时是 C/D 两组里唯一「不是给人手工维护用」的端点——触发方式是让模型批量猜边，
  没有界面就没有使用场景。`relRepo`/`posRepo` 字段保留（`structureContext` 仍在读）。

### E. Follow-up 生命周期（8 个）

`GET /follow-ups/:id`、`PUT /follow-ups/:id`、`GET /follow-ups/:id/postponements`、
`POST /follow-ups/:id/complete`、`/postpone`、`/wait`、`/cancel`、`GET /persons/:id/follow-ups`

- 涉及代码：`follow_up_handler.go` 的状态机、`follow_up_service.go` 的
  Complete/Postpone/ListPostponements、`follow_up_repo.go` 的 `Postpone`/`Complete`/`ListPostponements`
  与 **`follow_up_postponements` 延期历史表**
- 前端只调用了 `list` + `create`（EventDetail 页）
- **代价**：AI 建议采纳产生的事项永远停在 open。这是**写好但没接 UI 的功能**，
  首版审计就建议「补前端 UI 而不是删后端」——删除等于承认这个功能不做

### F. 备份 / 导出（6 个）

`POST /api/backups`、`GET /api/backups`、`GET /api/backups/status`、
`POST /api/backups/validate`、`POST /api/backups/restore`、`GET /api/export`

- 涉及代码：`handler/backup_handler.go` **整个文件**、`internal/backup/` 的
  `List`/`Status`/`ValidateFile`/`StageRestore`/`ExportJSON`/`ResolvePath`、`validate.go`、`export.go`
- **不受影响**：定时快照（`backup.Service.Start`）与启动时两阶段恢复（`ApplyPendingRestore`）
  走另一条代码路径，`backup.go` 与 `crypto.go` 留着照常工作
- ⚠️ **代价**：`scripts/drill.ps1` / `drill.sh` 的月度备份恢复演练**只通过这三个接口操作**
  （`POST /api/backups` → `/validate` → `/restore`），删了演练直接失效；
  手动恢复到某个历史快照也不再有任何入口

### G. 审计日志读取（1 个）

`GET /api/audit`

- 涉及代码：`handler/audit_handler.go` 及其读取方法
- **注意**：`AuditMiddleware` 仍在记录每一次变更，`service/maintenance.go` 仍按
  `maintenance.audit_retention_days` 修剪——**写入链路不能删**
- **代价**：审计日志变成「只写不读」，没有任何界面或接口能看到谁改过什么

## 四、顺带发现并已修掉的运维缺口

1. **release 管线已经断了（已修）**：`scripts/release.ps1` 与 `scripts/release.sh` 的冒烟
   检查要求 `POST /api/maintenance/cleanup` 返回 200，但该路由**早已不存在**（阶段 6 改造
   时改成只在启动时跑一遍）→ 发布会在冒烟这一步 exit 1。两处检查已删除。
   顺带发现 `release.sh` 还有第二处同类问题：`GET /api/nonexistent` 会被 SPA catch-all
   命中、返回 `200 text/html`，「404 包络」断言永远过不了；已改成
   `GET /api/persons/does-not-exist`，与 `release.ps1` 对齐。同时 `docs/RELEASE.md`、
   `readme.md`、`decisions.md`（ADR-010 第 1、4 条）里残留的手动清理说法已改为
   「只在启动时执行，无手动入口」。
   **（2026-09-14 后续）** 上面这条绕行只是绕过症状：SPA 兜底把未匹配的 `/api` 路径
   变成 200 + HTML，本身是接口契约的漏洞。根因已在 `internal/app/errors.go` 修掉
   （任何 `/api` 失败都走统一包络，未匹配路径 404、错误方法 405），两个发布脚本的
   绕行注释与断言随之更新——现在两条路径都断言，不再需要避让。
2. **备份健康状况没有出口**：`GET /api/backups/status` 的注释写明它存在的理由是
   「自动备份失败有明确可见提示」，但前端从未调用 → 备份静默失败时无人知晓。
   这属于「差一个界面」，不是「没用」——和 E 组同一类问题，端点按决定保留。

## 五、参数级未用能力

`GET /api/persons` 支持 `q` / `org_id` / `relation` / `sort` / `limit` / `offset`
六个服务端参数 + `X-Total-Count` 分页协议；前端 **8 处调用全部是裸 `personApi.list()`**
（Home / Report / Organizations / Events / EventDetail / Advice / CommandPalette / QuickRecordModal），
服务端筛选与分页参数连同 `PersonListParams` 类型实际从未使用。
个人 CRM 量级下留着无成本，删掉则要顺带简化 `person_repo.ListFiltered` 的查询拼装。

## 六、前端未使用的 API 封装（与端点同生共死）

处置原则：**后端端点被删的封装一律删，端点保留的封装一律留**（即使当前零调用——
它是对应能力的客户端存根，端点还在就是「未接 UI」而不是死代码）。

| 封装 | 状态 | 处置 |
|---|---|---|
| `organizationApi.{archive,restore}` | 零调用 | **已删**（端点已删） |
| `aiApi.inferHierarchy` | 零调用 | **已删**（端点已删） |
| `auditApi.list` | 零调用 | 保留（G 组端点保留） |
| `organizationApi.{members,addMember,endMembership,removeMembership}` | 零调用 | 保留（B 组端点保留） |
| `positionApi.create` | 零调用（`byPerson` 在用） | 保留（B 组端点保留） |
| `relationshipApi.{types,create,update,remove}` | 零调用（`byPerson` 在用） | 保留（A 组端点保留） |
| `followUpApi.{byPerson,get,postpone,complete,wait,cancel,postponements}` | 零调用（`list/create` 在用） | 保留（E 组端点保留） |
| `types.ts` 的 `Relationship` | — | 保留：仍是 `RelationshipLink` 的父接口 |

## 七、处置结果

| 组 | 决定 | 说明 |
|---|---|---|
| A 关系写接口（4） | **保留** | 前端零调用，但是唯一的 curl 手工维护通道；删了这张表就再无写路径 |
| B 任职写接口（4） | **保留** | 同上 |
| C 组织归档（2） | **已删** | 连同 handler/service/repo 方法与归档测试 |
| D AI 推断上下级（1） | **已删** | 唯一入口（图谱页）已下线；生产代码不再引用它 |
| E follow-up 生命周期（8） | 保留 | 状态机与延期历史是完整实现，缺的是界面；删除等于放弃这个功能 |
| F 备份/导出（6） | 保留 | drill 演练依赖，且是数据安全最后手段 |
| G 审计读取（1） | 保留 | 写入一直在发生，没有读取入口属于「未做完」而非「没用」 |

净结果：路由总数 65 → **62**。`routes.golden.txt` 与 `docs/architecture/api.md` 已同步。

## 八、遗留项（未处理，建议单独排期）

1. **`archived_at` 成为只读字段**：C 组删除后，`organizations.archived_at`、
   `?include_archived=true` 与前端「已归档」渲染都只剩读路径。要么补一个归档按钮，
   要么连字段一起清掉（需要迁移）。
2. **E 组与 `GET /api/backups/status` 都在等界面**：后端完整、前端没接。
   这类「差一个界面」的东西建议一次性补齐，而不是继续放着。
3. **`GET /api/persons` 的服务端筛选/分页参数**（`q`/`org_id`/`relation`/`sort`/`limit`/
   `offset` + `X-Total-Count`）前端 8 处调用全是裸 `personApi.list()`，见第五节。
