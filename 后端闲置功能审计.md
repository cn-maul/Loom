# 后端闲置功能审计（前端未调用部分）

> 审计日期：2026-09-13　|　基线提交：`f81987a`
>
> 方法：以 `internal/app/router.go` 注册的全部 60 个 API 端点为清单，逐一与
> `web/src`（React 前端）的实际调用比对。前端所有 HTTP 请求都经过
> `web/src/api/client.ts`，无绕过 client 的直接 `fetch`，因此比对是完备的。
> 已排除仅出现在 `_test.go` 中的引用。

---

## 一、整组未被前端调用的端点

### 1. 备份 / 导出组（6 个端点，整组零调用）

| 端点 | 用途 |
|---|---|
| `POST /api/backups` | 手动创建快照 |
| `GET /api/backups` | 列出快照 |
| `GET /api/backups/status` | 快照状态 |
| `POST /api/backups/validate` | 校验快照可恢复 |
| `POST /api/backups/restore` | 两阶段恢复 |
| `GET /api/export` | 全量 JSON 导出 |

- 涉及代码：`internal/handler/backup_handler.go`、`internal/backup/`（backup.go、crypto.go、export.go、validate.go）
- **重要前提**：`backup.Service.Start()`（app.go:148）按 `config.yaml` 的
  `backup.interval_hours` **后台定时快照**，与 HTTP 端点无关；启动时的
  `ApplyPendingRestore`（app.go:56）是恢复的第二阶段落地。即使删掉全部 6 个
  端点，定时备份仍然照常工作。
- 删除的真正代价：**"手动恢复到某个历史快照"将没有任何入口**（只能靠
  手工替换数据库文件）。
- 前端唯一接触点：Settings 页展示 `/config` 返回的 `backup_encrypted` 文字
  （"已开启（AES-256-GCM）"），这只是读配置，不依赖上述端点。

### 2. `POST /api/maintenance/cleanup`

- 涉及代码：`internal/handler/maintenance_handler.go`、`internal/service/maintenance.go`
- **注意**：启动时 app.go:158 已自动执行一次 `Cleanup()`（清理孤儿向量、
  按保留期裁剪审计日志）。服务本身**不能删**，闲置的只是"手动触发"端点。

### 3. `GET /api/tasks`（任务队列状态）

- 涉及代码：`internal/handler/tasks_handler.go`
- 队列本身（`internal/tasks`）是 AI 提取的核心，绝不能动；闲置的只是
  "查看队列健康状态（成功/失败/耗时计数）"这一个只读端点。

### 4. `GET /api/audit`（审计日志查询）

- 涉及代码：`internal/handler/audit_handler.go`
- **注意**：`AuditMiddleware` 仍在记录每一次变更操作，且 maintenance 依赖
  audit 表做保留期清理——**写入链路不能删**。删端点的后果是审计日志变成
  "只写不读"，没有任何界面能看到谁改过数据。

### 5. `GET /api/dashboard/stats`（首页聚合统计）

- 涉及代码：`internal/handler/dashboard_handler.go`、`internal/service/dashboard_service.go`
- Home 页实际是用 `eventApi.list`（近 12 条 + 本周计数）和 `personApi.list`
  自己拼的统计，没有走这个聚合端点。

---

## 二、已用功能组里未被调用的端点

### 6. 关系模块的 3 个查询端点

| 端点 | 说明 |
|---|---|
| `GET /api/relationships` | 全量关系列表（带筛选）。前端画布走 `GET /api/graph`，单个资料页走 `GET /persons/:id/relationships`，此端点无人用 |
| `GET /api/relationships/co-attendance` | **与 /graph 重复**：`/graph` 返回的 `co_attendance` 字段已把共同出席对送达前端（Relationships 页就在用它） |
| `GET /api/relationships/:id` | 单条查询。前端更新/删除都直接用列表里已有的对象 |

### 7. 单资源查询端点（REST 补全性质）

- `GET /api/positions/:id` — client.ts 的 `positionApi` 没有 get 方法
- `GET /api/organizations/:id` — client.ts 的 `organizationApi` 没有 get 方法

### 8. Follow-ups 生命周期（7 个端点，前端只用 list + create）

前端（EventDetail.tsx）只调用了 `followUpApi.list()` 和 `followUpApi.create()`。
以下端点全部零调用：

- `GET /api/follow-ups/:id`
- `PUT /api/follow-ups/:id`
- `POST /api/follow-ups/:id/complete`（完成）
- `POST /api/follow-ups/:id/postpone`（推迟，含延期历史表）
- `POST /api/follow-ups/:id/wait`（转等待）
- `POST /api/follow-ups/:id/cancel`（取消）
- `GET /api/follow-ups/:id/postponements`
- `GET /api/persons/:id/follow-ups`（按人查询，资料页也没用）

**这一组不建议删，反而是前后端不对等的缺口**：AI 建议采纳（`/advice/:id/adopt`）
会在后端生成 follow-up，EventDetail 里能看到，但整个 UI 没有任何"完成 /
推迟 / 取消"的入口——数据只会永远停留在 open 状态。要么补前端 UI，要么这
个功能整体就是半成品。

---

## 三、参数级未用能力

- `GET /api/persons` 支持 `q` / `org_id` / `relation` / `sort` / `limit` /
  `offset` 共 6 个服务端参数 + `X-Total-Count` 分页协议。前端 5 处调用全部是
  裸 `personApi.list()`，全量拉回后本地过滤（client.ts 里的 `listPaged`
  封装也是死代码）。
- 数据量为个人 CRM 量级时无性能问题；这些参数留着没有维护成本，删掉则
  会连带 `repository` 层的查询拼装一起简化。

---

## 四、前端侧顺带发现的死代码

`web/src/api/client.ts` 中定义但零调用的封装：`auditApi`、`dashboardApi`、
`personApi.listPaged`、`positionApi` 的 `update`/`remove`（仅 `byPerson` 被使用）
等。若决定删后端端点，这些应一并清理。

---

## 五、逐项处置建议（供决策）

| # | 项目 | 建议 | 理由 |
|---|---|---|---|
| 1 | 备份/导出 6 端点 | **保留**（或至少留 restore + export） | 定时备份虽在跑，但手动恢复/导出是数据安全的最后手段；删了就没有任何恢复入口 |
| 2 | `POST /maintenance/cleanup` | 可删端点，留服务 | 启动时已自动执行；手动触发仅排障用 |
| 3 | `GET /api/tasks` | 可删可留 | 只读状态端点，排障有价值，成本几乎为零 |
| 4 | `GET /api/audit` | 建议保留 | 审计写入一直在发生，没有查看入口属于"未做完"而非"没用"；删端点后日志只写不读 |
| 5 | `GET /api/dashboard/stats` | **可删**（或让 Home 改用它） | Home 已自行实现等价逻辑，二者必留其一 |
| 6 | `GET /relationships` + `GET /relationships/:id` | **可删** | /graph 与 /persons/:id/relationships 已覆盖全部场景 |
| 7 | `GET /relationships/co-attendance` | **可删** | 与 /graph 的 co_attendance 字段完全重复 |
| 8 | `GET /positions/:id`、`GET /organizations/:id` | 可删 | REST 完整性端点，前端从不用单查 |
| 9 | Follow-ups 生命周期 7 端点 | **不删，建议补前端 UI** | 后端状态机完整，前端缺操作入口，属于功能缺口 |
| 10 | `/persons` 筛选/分页参数 | 可留可删 | 删了可简化 repository，留了无成本 |

## 六、若决定删除，需要注意

1. `internal/app/routes.golden.txt` 是路由快照测试文件，删端点必须同步更新，
   否则 `router_test` 会失败。
2. 对应的 handler 方法、service 方法、repository 方法与 `*_test.go` 要成串删除；
   第 2 项（maintenance）和第 4 项（audit）**只能删 HTTP 端点**，底层服务有
   启动时调用和中间件依赖。
3. `web/src/api/client.ts` 中对应的封装与类型（`api/types.ts`）需同步清理。
