# Loom 实施进度与剩余工作

本文件记录本轮改造进度；总体目标和页面职责见《全站结构与后端能力规格.md》。
最近更新：2026-09-11（第一轮修跟进事项、第二轮恢复可编译、第三轮结构化关系与多人记录、
第四轮跟进事项闭环与记录提取状态、第五轮建议持久化与证据映射、第六轮周报重做、
第七轮跟进事项前端、第八轮组织管理补全与组织页、第九轮人物检索与聚合——后端功能全部落地、
第十轮关系图谱页、第十一轮记录列表与记录详情、第十二轮人物详情补齐与画像失效提示）。

## 当前基线（已实测）

```
gofmt -l ./cmd ./internal   → 干净
go vet ./...                → 通过
go test ./...               → 全绿（db / ai / repository / service / handler 均有测试，99 个用例）
go build ./...              → 退出 0
web: tsc -b && vite build   → 通过
```

- **构建链**：`//go:embed all:dist` 按包目录解析，找的是 `internal/frontend/dist`。
  首次构建必须 `cd web && npm install && npm run build`，再把 `web/dist` 复制成
  `internal/frontend/dist`，最后 `go build`。`build.ps1 / build.bat / build.sh` 已按此顺序；
  这两个 dist 目录都是产物，已在 `.gitignore` 中。
- **迁移**：新增 `schema_migrations` 版本表与有序迁移执行器（`internal/db/migrate.go`）。
  每个版本只执行一次，重复启动安全；迁移结束跑 `PRAGMA foreign_key_check` 验证引用完整。
  当前版本：v2 事件表重建、v3 关系/任职/参与人三表、v4 旧数据回填、
  v5 跟进事项闭环字段 + 记录提取状态 + 画像来源失效标记、
  v6 建议会话表 + 事项的来源建议列、v7 报告快照表。
- **记录流水线**：记录先落库再交给模型，所以「提取失败」是记录上的一种可见状态
  （`pending` / `succeeded` / `failed`），不是一个空摘要；人工修订过的记录默认拒绝被重试覆盖。
- **建议是持久对象**：一次生成落库成一条会话（问题、目标、回答、用到的上下文、模型、
  依据记录的内容指纹），可再次打开；逐条结论各自带依据，依据变了会被读时判定为失效。
- **报告是两层结构**：先从数据库聚合出结构化事实，再让模型只负责叙述；叙述失败只损失
  措辞，报告照常落库并标明原因。窗口规则在后端解析，未完成事项分五个互斥桶，
  明细全部带可跳转 id。
- **错误分类**：仓储/服务返回领域错误，handler 经 `classifyError` 映射为
  400 / 404 / 409 / 500；新增 `ORG_NOT_FOUND`，`PERSON_NOT_FOUND` 区分父对象缺失。

## 一、已完成

### 1. 跟进事项基础正确性（第一轮）

- `completed_at` / `description` / `due_date` 等可空列全部改走 `sql.NullString`，
  修掉「读未完成事项即 500」。
- 修正状态转换：延期只回到 `pending`（不再等同「等待对方」），已完成/已取消拒绝延期；
  重复完成幂等，不刷新完成时间；`Update` 用 `CASE WHEN` 让状态与 `completed_at` 一致。
- `Get / Update / Postpone / Action` 用行数校验拦不存在 ID，不再对幽灵 ID 返回成功。
- 延期接口接收 `{"due_date":"2026-10-01"}`，校验 YYYY-MM-DD。
- 专项测试：空值读取、缺失行、无期限排序、完成幂等、状态转换冲突、非法输入、
  期限范围过滤、9 个状态码分类用例。

### 2. 结构化人物关系 / 组织任职 / 多人记录（第三轮）

新增三张表（定义见 `internal/db/migrate.go`，也可看 readme 第七章）：

| 表 | 用途 | 关键约定 |
|---|---|---|
| `person_relationships` | 人物—人物关系边 | 方向、类型、起止时间、来源记录、确认状态 |
| `person_org_positions` | 人物—组织任职 | 同一人可多组织多段；`end_date` 非空即已离任，历史保留 |
| `event_participants` | 记录参与人 | 主键 (event_id, person_id)，一条记录只存一次 |

- **事件表重建**：`events.person_id` 由 `NOT NULL ... ON DELETE CASCADE` 改为可空
  `ON DELETE SET NULL`，并新增 `record_type` / `channel`。原约束会让「删除一个参与人」
  连带删掉其他人也参与的记录；SQLite 无法改约束，故整表重建（版本 v2）。
- **旧数据回填（版本 v4，带 NOT EXISTS 幂等保护）**：旧 `org_id` + `position` 迁移成一段
  起止时间未知的任职；旧单人物记录迁移为一条 `role='primary'` 的参与人。不复制事件，
  不伪造日期。
- **接口**：
  - 关系：`POST/GET/PUT/DELETE /api/relationships`、
    `GET /api/relationships/types`、`GET /api/relationships/co-attendance`、
    `GET /api/persons/:id/relationships`；列表支持
    `person_id / type / confirmed / active / q / limit / offset`，`person_id` 命中边的任一端。
  - 任职：`POST /api/persons/:id/positions`、`GET /api/persons/:id/positions`、
    `GET /api/organizations/:id/members?current=true`、`PUT/DELETE /api/positions/:id`。
  - 参与人：`GET/PUT /api/events/:id/participants`、
    `DELETE /api/events/:id/participants/:personID`。
  - 记录检索：`GET /api/events?person_id=&from=&to=&q=&limit=&offset=`；
    `GET /api/persons/:id/events` 现在也返回「以参与人身份参加」的记录。
  - 人物列表的活动统计改为「锚定人或参与人」都计一次（`COUNT(DISTINCT e.id)`）。
- **「我」的表达**：`persons.is_self`，全局最多一人（部分唯一索引 + 事务内先降级旧的）。
- **共同经历**：由参与人自连接派生，只用于图上展示，绝不自动写成朋友/同事/上下级关系边。

### 3. 跟进事项闭环与记录提取状态（第四轮）

**跟进事项不再是「标题 + 期限 + 状态」**，而是记住来源、负责人、原话和结果：

| 新增 | 用途 |
|---|---|
| `source_event_id` | 事项出自哪条记录（可空，`ON DELETE SET NULL`） |
| `owner` | 谁的球：我 / 对方 |
| `due_text` | 期限原文（「下周三前」），与确认后的 `due_date` 并存 |
| `completion_note` / `completed_event_id` | 完成结果，以及完成时写下的那条新记录 |
| `follow_up_postponements` | 延期历史（原期限 / 新期限 / 原因 / 时间） |

- 新建时校验被引用的记录存在；创建时传入的结果字段被忽略（未完成的事项不该有结果）。
- 已完成事项重复完成不刷新 `completed_at`，但允许补记后到的结果；一旦改回未完成，
  结果三件套（`completion_note` / `completed_event_id` / `completed_at`）一并清空。
- 新增 `POST /api/follow-ups/:id/wait`——此前 `waiting` 只能靠 `PUT` 设置。
- `GET /api/follow-ups/:id` 附带 `postponements`；另有独立的历史接口。

**记录提取状态与人工修订保护**（`events` + `traits`）：

- 新增 `extraction_status`（`pending` / `succeeded` / `failed`）、`extraction_error`、
  `extracted_at`、`manually_edited`、`edited_at`。记录落库即为 `pending`；
  失败的重试只报告这次尝试，**不清空原有内容**。
- `PUT /api/events/:id` 视为人工修订。此后重试接口返回 409，除非显式 `{"force": true}`；
  强制重试成功会清零 `manually_edited`（内容已是机器产出），失败则人工内容与标记都保留。
- 新增 `POST /api/events/:id/extract` 重试接口；`GET /api/events?status=` 可按提取状态筛选。
- `traits` 新增 `source_stale` / `source_stale_reason`：来源记录被改写原文或删除时，
  引用它的画像被标记而非丢弃；画像重新推导成功后标记自动清除。

### 4. 建议持久化与证据映射（第五轮）

建议此前只是一个响应体：页面一刷新，问过什么、用哪个模型、依据了哪些记录全都没了。
现在一次生成落库成一条建议会话（`advice_sessions`，迁移 v6），并且逐条结论各自带依据。

| 新增 | 用途 |
|---|---|
| `goal` | 用户想达成的目标，与问题分开存：同一个问题、不同目标，该说的话不一样 |
| `answer` | 完整回答的 JSON，含逐条结论与各自的 `evidence_event_ids` |
| `used_event_ids` / `used_trait_ids` | 实际放进提示词的记录与画像（是引用的超集） |
| `evidence_version` | 每条依据记录在生成时的内容指纹 |
| `retrieval_status` | 语义检索这一支的结果，四态 |
| `adopted_strategy_index` / `_name` | 采纳了哪一条策略 |

- **逐条证据**：`situation` / `other_perspective` / 每条 `risk` / 每条 `strategy` / `follow_up`
  各自带 `evidence_event_ids`。空数组表示「没有直接记录依据」，界面必须明说是推断。
  模型编造的 ID 在入库前被过滤，总依据由各条列表合并而来，两者不可能不一致。
- **检索三态分得开**：`vector_used` / `no_relevant_evidence` / `retrieval_failed` /
  `vector_disabled`。「没启用向量」「检索坏了」「确实没有相关记录」要做的事完全不同，
  不再塌缩成一个 `vector_used: false`。`VecRepo.Available()` 提供「索引是否可用」。
- **失效是读时算出来的**：`evidence_version` 存的是内容指纹（
  `raw_text/summary/感受/反应/承诺/日期` 的 FNV），不是时间戳。读的时候和当前行比对：
  对不上＝「来源记录已修改」，查不到＝「来源记录已删除」。所以不存在「标记写死了但对不上现状」
  的可能，记录改回原样也会自动恢复为有效。
- **采纳策略 = 转成跟进事项**：`POST /api/advice/:id/adopt` 把选定策略转成一条事项，
  话术写进事项描述、事项带 `source_advice_id`，会话上记下采纳的是哪一条。
- **删建议不删行动**：删除会话前先给引用它的事项写 `source_advice_stale` 与原因，再删
  （外键把 `source_advice_id` 置空）。与 `source_event_id` 的取舍相反是有意的：
  采纳的策略是行动的前提，「前提没了」值得留在事项上。

新增接口：`POST /api/advice`（生成并保存）、`GET /api/advice`（历史，支持 `person_id` /
`limit` / `offset`）、`GET /api/advice/:id`、`DELETE /api/advice/:id`、
`POST /api/advice/:id/adopt`。旧的 `POST /api/ai/advice` 已由 `POST /api/advice` 取代。

前端 `/advice` 页已接到新结构：可填目标、看到每条结论的依据（无依据时明说「无直接记录依据」）、
依据记录已删除时显示「记录已删除」、来源变化时顶部提示、每条策略可「采纳并转为跟进事项」、
下方是这个人物的建议历史（含「依据已变化」「已采纳」标记与删除）。

### 5. 跟进事项前端（第七轮）

新增 `/follow-ups` 页（侧边栏「跟进」），后端六轮沉淀的闭环能力第一次有了界面：

- **列表与筛选**：按人物、按状态（待办/等待对方/已完成/已取消）筛选；
  逾期事项红色徽标显示逾期天数；状态、负责人、期限原话、来源记录跳转齐全。
- **操作闭环**：完成（可填结果）、延期（可填原因）、等待对方、取消；
  每条事项可展开「延期历史」（原期限 → 新期限 · 原因 · 时间）。
- **手动新建**：不依赖记录提取，直接补一条事项（人物/标题/说明/谁的球/期限）。
- **入口打通**：人物详情页新增「跟进事项」卡片（逾期红框）；
  建议页「已转出 n 条」现在可点击跳转；报告页里的事项标题可跳转。
- SPA 深链接验证：`/follow-ups`、`/persons/:id` 直接刷新都能由 embed 层正确回退到 index.html。

### 6. 周报重做：结构化报告与快照（第六轮）

旧周报只有一段模型文字和两个计数。现在报告分两层：**先从数据库聚合出结构化事实，
再让模型只负责叙述**——模型坏了只损失措辞，不损失事实（定义详见 readme 7.4）。

| 新增 | 用途 |
|---|---|
| `report_snapshots`（迁移 v7） | 报告快照：窗口、状态、模型、摘要、完整分段 payload；`person_name` 冗余保存，人物删除后报告仍可读 |
| 窗口解析 `ReportRequest.Resolve` | 默认最近七天；`week_of` 展开为自然周（周一至周日）；`start`/`end` 必须成对、跨度上限 366 天；解析结果写回快照 |
| 五桶互斥分类 | `overdue` / `due_soon` / `waiting` / `carried_over` / `upcoming`，外加窗口内完成的 `completed`；`cancelled` 不进任何桶 |
| 明细引用 | 事项、记录、承诺、关系/任职变化全部带可跳转 id 与人名 |
| 按人物汇总 | 记录数、最近记录日期、未完成事项数，聚合时顺带统计，不加查询 |

- **聚合先于模型**：`collect()` 全部读库完成后再尝试叙述；叙述失败时报告照常落库，
  `status=failed`、`failure_reason` 写明原因、`summary` 退回大纲文本。
- **空窗口不请模型**：`generated_by=local` 直接作答，模型没有机会为空白期编故事。
- **大纲只带人名不带 id**：发给模型的文本里没有记录键，界面已把 id 渲染成链接。
- **历史只读 headline**：列表接口不取 payload，回看一年报告不会带出一年的分段数据。

新增接口：`POST /api/reports`、`GET /api/reports?person_id=`、`GET /api/reports/:id`、
`DELETE /api/reports/:id`。旧的 `GET /api/ai/report/weekly` 已下线。

前端 `/report` 页重建：人物范围、自定义起止 / 「上一周」快捷键、六段事项卡片
（逾期带天数徽标）、变化 / 承诺 / 记录 / 人物汇总分区、生成状态与失败原因提示、
报告历史快照（点击回看、可删除）。

### 7. 组织管理补全与组织页（第八轮）

组织此前只是「一个名字的分组标签」，藏在设置页里。现在（迁移 v8）：

| 新增 | 用途 |
|---|---|
| `organizations.kind` | 类型，用户自己的词汇（公司/学校/社团…自由文本） |
| `organizations.description` | 说明 |
| `organizations.archived_at` | 软归档时间戳；NULL 即活跃 |

- **归档不是删除**：归档后不出现在人物表单与首页筛选（`GET /api/organizations` 默认只返回
  活跃），但历史任职仍然指向它，人物列表的 `org_name` 照常解析；`include_archived=true`
  供组织页取全量。`POST /organizations/:id/archive` / `restore` 切换状态。
- **组织页 `/organizations`**（侧边栏「组织」）：左侧新建（名称/类型/说明）与分组列表
  （活跃在上、已归档折叠在下）；右侧详情编辑、归档/恢复/删除、成员管理——
  现任成员（人物跳转、职位、加入日期）、添加成员、结束任职（保留历史）、删除任职、历史成员。
- **设置页拆分**：设置页的「其他」页签不再管理组织，改为跳转链接，与规格书
  「组织管理与模型配置分离」对齐。
- 冒烟 15 项：元数据回读、成员/结束任职、归档后选择器不可见但 org_name 仍解析、
  恢复、404 分类、删除、SPA 深链接，全过；测试函数 84 → 86。

### 8. 收尾决策

- `FollowUp.CompletedAt` 改为 `*time.Time`：`omitempty` 对结构体无效，未完成事项此前会
  序列化成 `"0001-01-01T00:00:00Z"`，现在直接省略该字段。
- `.gitignore` 明确 `internal/frontend/dist` 属构建产物，并忽略 `data/`、根目录二进制。

### 9. 人物检索与聚合（第九轮，后端功能收官）

`GET /api/persons` 是最后一个不支持服务端检索的列表。现在：

| 参数 | 行为 |
|---|---|
| `q` | 关键字命中姓名 / 关系 / 职位 / 备注（LIKE） |
| `org_id` | 组织过滤；哨兵 `none` = 未归属 |
| `relation` | 关系精确匹配 |
| `sort` | `recent`（默认）/ `name` / `importance` / `created`，封闭词汇表 |
| `limit` / `offset` | 页窗口，`limit<=0` 不分页 |

- **总数放 `X-Total-Count` 头**：响应体保持数组形状，与记录/关系列表一致；
  前端用「请求 limit+1 条」判断下一页，不额外发计数请求。
- **未知排序返回 400**，不静默回退；负数 limit/offset 同样 400。
- 计数查询只扫 persons（过滤条件全在其列），列表查询仍 LEFT JOIN 出
  `last_event_date` / `event_count` / `org_name`；`ListWithActivity` 成为
  无过滤调用的薄包装，旧测试与调用方不受影响。
- 首页人物列表前端重做：搜索框防抖 300ms 走服务端、组织筛选改服务端
  （`未归属` → `org_id=none`）、四种排序下拉、「加载更多」分页；
  无记录人物明示「尚无记录」，不再拿创建时间冒充最近联系。
- 新增测试 9 个：仓储层搜索命中备注/职位、none 哨兵、关系精确匹配、四种排序、
  分页不重叠、服务层非法排序/负窗口拒绝；handler 层查询参数、X-Total-Count、
  未知排序 400。测试函数 86 → 95。

### 10. 关系图谱页（第十轮）

规格书 3 号页面的落地：谁与谁有什么关系，这个判断是否可靠。

**后端 `GET /api/graph`**（`models/graph.go` + `repository/graph_repo.go` +
`service/graph_service.go` + `handler/graph_handler.go`）一次返回整张画布：

| 分区 | 内容 |
|---|---|
| `nodes` | 全部人物（is_self / 组织 / 重要度 / 记录数，复用人物列表投影） |
| `orgs` | 活跃组织 + 现任成员数 |
| `edges` | `relationship`（含已结束，历史置灰）与 `position`（仅现任任职）两族 |
| `co_attendance` | 共同经历对，`participants UNION events.person_id` 自连接派生 |

- 锚定人经 UNION 计入共同经历：只给锚定人、没填参与人的记录也能把两人连起来。
- 共同经历永远是点线的「同场出现」，不渲染成关系边。
- 新增测试 1 个（图谱整体断言：节点/组织/两族边/共同经历/已结束任职不出边），95 → 96。

**前端 `/relationships` 页**（侧边栏「关系图谱」，无第三方依赖）：

- 轻量力导向布局（Fruchterman-Reingold 简化版，人物圆节点按记录数定大小，组织为方块）。
- 筛选：关系类型、组织、时间窗（边的起止与窗口相交即算）、含/不含已结束、共同经历开关。
- 点边看详情（方向、起止、确认状态、备注、来源记录跳转）；点节点看相关关系并跳人物/组织页。
- 添加 / 编辑 / 结束 / 删除关系；双击关系边直接进入编辑。结束 = 写入当天 end_date，历史保留。

### 11. 记录列表与记录详情（第十一轮）

记录此前只能从人物页时间线进入，也没有「提取失败待重试」的集中入口。

**后端**：`GET /api/events` 补 `X-Total-Count`（与人物列表同口径）。`EventRepo.ListFiltered`
改为返回 `(events, total, error)`，过滤条件抽成 `eventFilterClause`，**计数与列表共用同一段
WHERE**，不会出现总数与翻页错位；`limit<=0` 表示不分页。调用点同步：`ListByPerson`、
`EventService.List`、`ReportService.collect`。

**前端 `/events` 新页面**（侧边栏「记录」）：

- 关键词（防抖 300ms）、人物（锚定人或参与人）、时间窗、提取状态筛选；「加载更多」显示剩余条数。
- 每行带提取状态徽章（已提取 / 待提取 / 提取失败），失败行显示错误原因，可就地重试；
  人工修订过的记录重试前弹二次确认（force）。

**前端记录详情 `/events/:id` 重写**（原 137 行薄壳）：

- 参与人维护：带姓名与参与方式，可增删，重复添加去重，最后一位不可移除。
- 编辑：日期、形式、渠道、摘要、我的感受、对方反应、原文；保存置 `manually_edited`。
- 承诺一键转跟进事项（带 `source_event_id`），已转的不再重复提供；底部列出已转出的全部事项。
- **编辑必须原样回传 `promises`**：`PUT /api/events/:id` 只写收到的字段，省略会被清空。

**顺带修掉的真实缺陷**：`ai.NewClient` 在 `api_key` 为空时直接构造失败，导致默认配置
（本地 Ollama）下每次提取都在发请求前以 `api key is required` 失败——即用户上次启动看到
的「所有记录提取失败」。现在未配置时用占位值，本地端点开箱可用（`internal/ai/client_test.go` 覆盖）。

新增测试 2 个（记录列表分页与总数头；无 api_key 也能建客户端），96 → 98。

### 12. 人物详情补齐与画像失效提示（第十二轮）

人物详情此前只有头部档案、画像、跟进事项、记录与提问框，任职历史和组织背景无从查看，
画像「依据已变」的标记后端存了但前端没有呈现。

**前端人物详情 `/persons/:id`**：

- 新增「组织与任职」卡片：完整任职历史（组织、角色、起止、在职/已结束、备注），
  现任排在前；可新增任职、**结束任职只写结束日期保留历史**、删除任职；
  下方展示当前组织的类型/描述与同组织现任同事（可点进对方人物页）。
- 新增「关系」卡片：该人的关系列表，含方向（出/入）、类型、双向标记、未确认标记、
  起止日期与备注；可跳对方人物页与图谱页。空态明确说明「同场出现不算关系，不会自动推断」。
- 切换人物时清空提问框、编辑态与任职表单——否则上一个人的草稿会出现在下一个人页面上。

**画像失效提示**：`TraitList` 展示 `source_stale`（徽章「依据已变」+ 原因 + 跳来源记录重新提取的入口）。

**顺带修掉的真实缺陷**：`RetryExtraction` 成功后没有重新派生画像，于是「依据已变的画像」
永远无法被用户清除——按提示去重新提取，标记依旧挂着。现在重试会调用 `RefreshTraits`，
重新派生后标记清除（`report.TraitsUpdated` 同步返回）。配套测试断言
`UpdateExtraction` 不会重新标记画像，保证重试链条不被自我污染。

新增测试 1 个（重试重写提取结果不重新标记画像），98 → 99。

### 13. 前端整体美化（第十三轮）

用户反馈「布局与信息展示需要美化」，本轮对全部页面做了一轮统一版式改造，不改任何后端行为：

- 新建 `web/src/components/layout.tsx`：PageHeader（页面标题 + 说明 + 操作区）、
  StatCard（统计卡片）、SectionCard（分节卡片）三个通用件，统一间距、圆角、描边与字号层级。
- `App.tsx`：侧边栏按「概览 / 人物 / 记录 / 洞察 / 系统」分组，带分组小标题与活跃高亮；
  顶栏常驻「记一笔」快捷入口（QuickRecord 抽屉）。
- `Home.tsx`：顶部统计条（人物 / 记录 / 待办 / 组织四卡）+ 人物卡片网格（头像占位、
  组织与最近记录摘要）。
- `Events.tsx`：筛选区（状态 / 人物 / 时间）收进一行工具栏，列表改卡片流，状态徽章统一。
- `FollowUps.tsx`：按紧急度分桶（逾期 / 今日 / 即将 / 以后）+ 各桶计数统计条，
  逾期项红点强调。
- `EventDetail.tsx`：头部信息区 + 分节卡片（概要 / 画像 / 承诺 / 事项），保存按钮吸底。
- `PersonDetail.tsx`：头图区（姓名 / 关系 / 重要度徽章）+ 两栏布局（左侧画像与记录、
  右侧组织与关系）。
- `Relationships.tsx` / `Organizations.tsx` / `Report.tsx` / `Advice.tsx` / `Settings.tsx`
  统一加 PageHeader，卡片化空态。
- `EventTimeline.tsx` 重写为按日期分组的时间线样式；`QuickRecord.tsx` 与
  `PersonDetail.tsx` 的入口调用同步适配。
- `api/client.ts` 增补缺失的类型与端点封装（配合新页面取数）。

验证：`npm run build` 通过；`go build` 重编 relationship.exe（embed 新前端）通过；
真实二进制冒烟（首页 / persons / events / graph）全 200。

### 9. 测试与文档

- 新增测试：迁移建表/幂等/旧数据回填/删除锚定人保留共同记录（`internal/db/migrate_test.go`）；
  关系命名与筛选、结束关系保留历史、任职现任与历史、共享记录对每位参与人可见、
  活动统计、is_self 唯一（`internal/repository/structure_test.go`）；
  关系/任职/参与人的状态码分类与多参与人行为（`internal/handler/structure_handler_test.go`）。
- 第四轮新增：迁移 v5 建表/补列/提取状态回填（`migrate_test.go`）；
  记录提取状态与人工修订、画像来源失效标记（`internal/repository/record_state_test.go`）；
  延期历史、完成结果、改回未完成时清空结果、按提取状态筛选（`follow_up_repo_test.go`）；
  闭环字段与来源缺失校验（`internal/service/follow_up_service_test.go`）、
  来源改写/删除导致画像失效与人工修订保护（`internal/service/event_service_test.go`）；
  新增接口的状态码分类与人工修订冲突（`internal/handler/follow_up_handler_test.go`、
  `record_state_handler_test.go`）。
- readme 增补第七章 7.1「结构化关系、任职与多人记录」与 7.2「跟进事项闭环与记录提取状态」。
- 第五轮新增：迁移 v6 建表/补列/删建议后事项仍保留（`migrate_test.go`）；
  建议会话存取与结构往返、历史排序分页、采纳记录、事项来源标记、记录内容指纹随改随变
  （`internal/repository/advice_repo_test.go`）；生成落库、过滤编造依据、**检索四态**、
  来源改写与删除导致失效、采纳转事项、删建议后事项状态、
  纯函数级的依据归一化（`internal/service/advice_service_test.go`）；
  建议接口的读取/历史/采纳/删除与 400/404 分类（`internal/handler/advice_handler_test.go`）。
  服务层测试用 `httptest` 起了一个假模型端点，因此覆盖到了「提示词→嵌套 JSON→过滤→落库」整条链路。
- readme 增补第七章 7.3「建议持久化与证据映射」，并更新 11.3 的建议生成 Prompt。

## 二、验收实测（真实二进制 + 临时 SQLite + 真实 HTTP）

```
建人物（含 is_self）→ 建组织 → 建关系 → 建任职                  ✓
建一条三参与人记录（LLM 不可用）→ 记录仍落库，warning 正常返回   ✓
张总 / 李工 / 我自己 三人都能看到这条记录，各计 1 次活动          ✓
组织现任成员 =1 → 关闭任职 → 现任 =0、历史 =1                    ✓
graph: 张总的关系边 1 条、类型 =上级、共同经历 3 对，Top 我自己/张总 ✓
自环关系 → 400 INVALID_INPUT                                     ✓
未知人物 → 404 PERSON_NOT_FOUND；未知组织 → 404 ORG_NOT_FOUND    ✓
未知记录参与人 → 404 NOT_FOUND；清空参与人 → 400 INVALID_INPUT    ✓
删除一名参与人 → 200 且记录仍在；再删同一人 → 404                 ✓
active/maybe → 400 INVALID_INPUT                                 ✓
静态首页 GET / → 200 + 内嵌 index.html                           ✓
```

第四轮（跟进事项闭环 + 记录提取状态）同样跑了一遍真实二进制 + 临时库 + 真实 HTTP：

```
建记录（LLM 不可用）→ 记录仍落库，status=failed 且带错误信息        ✓
GET /events?status=failed → 1 条；?status=done → 400              ✓
建事项带 owner / due_text / source_event_id → 原样回读             ✓
延期（带原因）→ due 更新，历史 1 条：old=2026-09-16 → new=2026-09-23 ✓
完成（带结果与关联记录）→ status=completed + note + completed_event_id ✓
人工修订 → manually_edited=1；重试 → 409 CONFLICT                 ✓
强制重试（提取端不可用）→ 200 且 status=failed，人工内容未被覆盖    ✓
删除来源记录 → 事项保留、引用被置空（记录在案的取舍）              ✓
未知来源/结果记录 → 404；未知提取状态 → 400；未知事项历史 → 404    ✓
静态首页 GET / → 200                                             ✓
```

第五轮（建议持久化）用真实二进制 + 临时库 + 一个假的模型端点（真实 HTTP）跑完整链路，
28 项断言全过：

```
建人物 → 建记录（提取成功）                                        ✓
POST /advice → 201，返回会话 id                                    ✓
会话保存了问题与目标；逐条结论结构完整（风险 1、策略 2）            ✓
模型编造的依据 ID 被过滤；并入的依据就是那条真实记录                ✓
retrieval_status = vector_used（记录已入向量索引）                  ✓
evidence_version 记下了依据记录的指纹                              ✓
GET /advice/:id 可再次打开；GET /advice?person_id= 历史 1 条        ✓
未知建议 → 404 NOT_FOUND                                          ✓
采纳策略 → 201，事项带上策略话术、due_text，并回指 source_advice_id ✓
采纳越界策略 → 400 INVALID_INPUT                                   ✓
改写来源记录原文 → GET /advice/:id 与历史都变为 source_stale=1     ✓
删除建议 → 200；再取 404；事项仍在且 source_advice_stale=1         ✓
静态首页 GET / → 200                                              ✓
```

第六轮（报告）同样用真实二进制 + 临时库 + 假模型端点跑了 22 项断言，全过：

```
建人物 → 建今日记录（提取成功）→ 建昨日到期事项                     ✓
空 body 生成 → 201，窗口默认最近七天                                ✓
叙述来自模型（generated_by=mock-advice）                           ✓
记录带 id 引用；提取出的承诺进报告                                 ✓
昨日到期事项落入 overdue 且 days_overdue=1                         ✓
空分段是数组不是 null；按人物汇总计数正确                          ✓
week_of 展开为周一至周日                                          ✓
倒置窗口 → 400；未知人物 → 404                                     ✓
历史 2 条；快照可整读；人物过滤生效                                 ✓
删除 → 200；再取 → 404；静态首页 → 200                             ✓
```

第九轮（人物检索）用真实二进制 + 临时库 + 真实 HTTP 跑了 19 项断言，全过：

```
建组织/4 人物/2 记录（提取端不可用仍落库）                          ✓
默认列表 4 人，X-Total-Count=4，org_name 解析                      ✓
排序：有记录者按最后记录倒序，无记录者回退创建时间                  ✓
q=登山 → 命中备注 → 陈静；q=工程师 → 命中职位 → 李工               ✓
org_id=腾讯 → 2 人；org_id=none → 未归属 2 人                      ✓
relation=朋友 精确匹配 → 陈静                                      ✓
sort=importance/name/created 全部可用且确定性                      ✓
limit=2&offset=2 两页无重叠，总数头正确                            ✓
sort=popularity → 400 INVALID_INPUT                               ✓
GET / 与 SPA 深链接 /persons → 200 内嵌 index.html                 ✓
```

第十轮（关系图谱）同样真实二进制 + 临时库 + 真实 HTTP，18 项断言全过：

```
建组织/3 人物/2 记录/1 任职/1 关系（提取端不可用仍落库）             ✓
GET /graph 200：3 人物节点（is_self、org_name）、1 组织节点（现任=1）✓
关系边 1 条带类型/方向/确认；任职边 1 条（我→腾讯）                 ✓
共同经历 3 对，我×张总 = 2（锚定人经 UNION 计入）                   ✓
/relationships/types 含「同事」                                    ✓
结束关系 → 图谱边带 end_date（历史保留）；删除关系 → 边消失         ✓
自环关系 → 400                                                     ✓
SPA 深链接 /relationships 与 GET / → 200 内嵌 index.html           ✓
```

第十一轮（记录）真实二进制 + 临时库 + 假模型端点，27 项断言全过：

```
建 3 人物 → 建多人记录（提取成功、参与人补到 3 位、承诺落库）        ✓
X-Total-Count=3；首页 2 条按记录日期倒序；第二页 1 条无重叠         ✓
q=排期 命中原文；status=succeeded 过滤；未知状态 → 400              ✓
person_id=参与人 → 共同记录可见；from=2026-09-05 时间窗生效         ✓
参与人：重复去重 → 3 位；移除 → 2 位；列表带姓名                     ✓
编辑 → manually_edited=1，承诺未被清空，形式/渠道写入                ✓
普通重试 → 409；force 重试 → 成功并清零人工标记、摘要回到模型结果    ✓
承诺转事项 → 201，source_event_id 可回读                            ✓
删除记录 → 200，再取 → 404；SPA 深链接 /events → 200                ✓
```

第十二轮（人物详情）真实二进制 + 临时库 + 假模型端点，24 项断言全过：

```
任职新增 201（带出 org_name/person_name）；历史任职排在当前之后      ✓
结束任职 → end_date 写入；current 成员过滤生效、历史仍在             ✓
删除任职 → 列表收缩为 1 条                                          ✓
建关系 → 按人查询带出双方姓名；active=true 保留未结束关系           ✓
建记录 → 提取成功；画像生成并引用该记录                             ✓
编辑记录原文 → 画像 source_stale=1 且带原因                         ✓
force 重试 → 画像重新派生，失效标记清除                             ✓
SPA 深链接 /persons/:id 与 GET / → 200 内嵌 index.html              ✓
```

## 三、剩余工作

后端功能已全部落地。前端页面已全部接入（首页、人物详情、记录列表、记录详情、
跟进、报告、建议、组织、关系图谱），剩余为收尾：

### 收尾

- 浏览器实测（前端须在开发服务或构建产物中人工过一遍主流程）。
- 建 Git 仓：目前十二轮改动均在工作区未建仓；推送前必须确认目标仓库与发布范围，
  不得提交 `config.yaml` 实际密钥、`data/` 真实数据库、可执行文件与构建产物。

## 四、已知取舍（供后续决定）

- `persons.org_id / position` 与 `person_org_positions` 并存：旧字段仍是人物档案上的展示值，
  任职表是组织成员列表的事实来源。迁移只做一次性回填，之后两者不同步（因为同步需要在
  离任时写一个真实日期，而旧档案并未记录）。若希望收敛为单一事实来源，需先决定
  「清空人物组织」时如何表达离任时间。
- `event_participants.role` 中 `primary` 由系统补齐给锚定人；用户自填的参与方式原样保留。
- `events.person_id` 保留为「锚定人」，API 仍可用 `person_id` 单人物方式创建记录（向后兼容）。
- 跟进事项的 `source_event_id` / `completed_event_id` 用 `ON DELETE SET NULL`：删掉记录不会
  连带删掉事项，但引用会静默断开。画像已经用 `source_stale` 表达了「依据已变」，
  事项若要同样保留这个信息，需要再加一列；本轮先按「不留悬空 ID」处理。
- `manually_edited` 描述的是「库存提取结果由谁写的」：强制重试成功后清零。若希望
  「人工碰过就永久保护」，需要改成只增不减的标记。
- 记录的人工修订保护只覆盖提取结果；`raw_text` / `event_date` 的修改没有版本历史，
  改写原文后无法回看旧文本（只有画像会收到 `source_stale` 提示）。
- 建议的失效判定建立在**内容指纹**上，所以「改回原样」会被判为有效，「改了但语义没变」
  会被判为失效。这是有意的：比起猜语义，宁可对一个无实质影响的改动也提示一次。
- 建议的失效**不会**传播给它转出的跟进事项：事项只在建议被删除时收到标记。若认为
  「建议失效了，据此做的行动也该被提醒」，需要在读事项时联动判断，目前未做。
- `evidence_version` 只记事件 ID 与指纹，不记画像版本；回答引用的画像（`used_trait_ids`）
  后续被重新推导时，建议不会因此失效。要覆盖这一点需要给画像也做内容指纹。
- 建议回答的 `risks` 由 `[]string` 变成 `[]AdvicePoint`（每项带 `text` 与
  `evidence_event_ids`），`POST /api/ai/advice` 由 `POST /api/advice` 取代：
  这是为了让「每条结论都能打开依据」成立而做的破坏性改动，前端已同步。
- 报告快照写入即定格，不会随数据变化自动重算：这是「回看历史时看到当时的样子」的代价。
  想要最新结论就重新生成一份。
- 报告的 `due_soon` 判定以窗口结束日为界：对过去的窗口，本期内到期但已过期的事项
  会落入 `overdue`（逾期是事实，不因窗口是历史窗口而改写）。
- 报告里的事项引用目前只有 id 与文本，没有事项状态流转（完成/延期）要回到跟进事项页操作；
  等事项前端接入后补上直达链接。
- 采纳策略时策略话术被写进事项 `description`。若用户之后编辑建议（目前不支持编辑），
  或想分别保留多条策略，事项上的文字不会跟着变。

## 五、验收与发布

- 后端测试覆盖：空值、无效日期、不存在 ID、重复操作、状态转换、日期筛选、迁移幂等与旧数据回填。
- 前端须启动开发服务并在浏览器验证：正常流程、错误状态、切换人物。
- 当前项目根目录**不是 Git 仓库**（`.git` 不存在，无远端）；`go.sum` 的历史冲突标记已清理。
- 推送前必须确认目标仓库与发布范围；不得提交 `config.yaml` 实际密钥、`data/` 真实数据库、
  可执行文件、`node_modules` 与构建产物（`web/dist`、`internal/frontend/dist` 均已在忽略列表）。
