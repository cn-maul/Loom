# Loom 实施进度与剩余工作

本文件记录本轮改造进度；总体目标和页面职责见《全站结构与后端能力规格.md》。

## 当前进度

已新增跟进事项表、基础模型、仓储、service、handler 和路由：

- GET /api/follow-ups
- POST /api/follow-ups
- GET /api/follow-ups/:id
- PUT /api/follow-ups/:id
- POST /api/follow-ups/:id/complete
- POST /api/follow-ups/:id/postpone
- POST /api/follow-ups/:id/cancel
- GET /api/persons/:id/follow-ups

延期接口接收 `{"due_date":"2026-10-01"}`，校验 YYYY-MM-DD 日期，并保存截止日期、设置 waiting 状态、清除完成时间。

最近一次 gofmt 和 go test ./... 已通过，但这仅证明当时代码可编译且已有测试通过。当前没有跟进事项专项测试，不能据此认定接口已完成行为验收。前端尚未接入跟进事项。

## 先修复和验证的基础问题

- 仓储读取 completed_at 时必须正确处理 NULL；目前延期操作写入 NULL 后，详情读取可能失败。description、due_date 等可空字段也应核查。
- 更新、完成、取消、延期必须检查目标是否存在，不应对不存在的 ID 返回成功。
- 区分输入错误、目标不存在、数据库异常，分别返回适当 HTTP 状态。
- 统一截止日期和查询范围校验。
- 明确状态转换：延期不应无条件重新打开已完成或已取消的事项；延期与等待对方不是同一业务含义。
- 更新状态时同步维护 completed_at，并保证重复完成操作的幂等性。
- 核查按人物查询处理器，不应修改 Echo 路由路径来查询数据。
- 使用临时 SQLite 数据库补充集成测试，不操作真实数据库。

## 后端剩余能力

### 1. 跟进事项

- 来源记录、负责人、期限原文。
- 完成结果、完成时关联的新记录。
- 延期历史：原期限、新期限、原因、操作时间。
- 状态转换和幂等规则。
- 来源建议关联在建议持久化实现时一并接入，避免悬空 ID。
- 事项与记录、建议、周报之间的闭环。

### 2. 人物关系

- 结构化关系表与 CRUD API。
- 双方人物、关系类型、方向、起止时间、来源记录、确认状态、备注。
- 明确“我”的主体表达。
- 结束关系保留历史；旧 relation 文字不得直接推断成关系边。

### 3. 组织任职

- 人物—组织任职表，多组织和历史任职。
- 职位、起止日期、来源、备注。
- 当前和历史成员查询。
- 旧 org_id、position 的迁移方案，未知时间不伪造。

### 4. 多人记录

- event_participants 表与参与人维护 API。
- 同一记录关联多个人物，不复制事件。
- 参与角色、记录类型、渠道。
- 按参与人查询及人物活动统计。
- 处理现有 events.person_id 删除级联对多人记录的影响。

### 5. 提取状态和修订

- 提取状态、失败原因、时间和版本。
- 编辑原文、日期和提取结果。
- 人工修订标记及重试保护。
- 提取重试接口。
- 来源修改或删除后，对衍生画像和建议标注影响。

### 6. 建议

- 问题、目标、上下文、回答、模型和生成时间持久化。
- 逐条结论映射到来源，而不只是整份回答附事件 ID。
- 展开画像来源，区分事实、推测和未知。
- 区分检索失败与没有相关证据。
- 策略采纳、转事项、执行结果回写。

### 7. 周报

- 明确起止日期且查询包含结束上界。
- 跨周未完成事项、逾期和本期到期分类。
- 事件和事项 ID 明细，支持打开来源。
- 报告快照和历史查询。
- 历史报告内容与事项当前状态分开呈现。

### 8. 数据迁移和查询

- 可重复执行、可验证的迁移及旧数据回填测试；版本化迁移尚未实现。
- 搜索、分页和聚合接口。
- 保留既有原文，避免迁移时丢失或重复数据。

## 前端剩余工作

- 人物列表：搜索、筛选、最近记录摘要、未完成事项，区分最近记录与最近联系。
- 人物详情：概览、记录、关系、画像分区，接入跟进与任职历史。
- 关系图谱：当前尚无页面，待结构化关系 API 完成后实现。
- 记录列表与详情：全局检索、多人参与、编辑、提取重试、承诺确认及转事项。
- 建议：历史、选定上下文、事实与推测、逐条依据、采纳和执行结果。
- 周报：日期范围、跨周事项、状态操作、明细来源和下周安排。
- 组织：从设置拆出独立页面，展示当前及历史成员。
- 设置：拆出业务管理，区分配置草稿、已保存配置及运行状态。
- 跨页面状态：切换人物时隔离草稿和旧结果，补录记录正确排序。

## 推荐实施顺序

1. 修复跟进事项基础正确性，增加专项测试。
2. 补跟进来源、完成结果和延期历史。
3. 实现结构化人物关系、组织任职和多人记录及迁移。
4. 重做人物、记录和组织页面并接入事项。
5. 实现建议保存、证据映射和行动回写。
6. 重做周报，完成跨周跟进闭环。
7. 实现图谱，整理设置。

## 验收与发布

- 后端测试覆盖空值、无效日期、不存在 ID、重复操作、状态转换、日期筛选和旧数据迁移。
- 前端须启动开发服务并进行浏览器验证，覆盖正常流程、错误状态和切换人物场景。
- 当前项目根目录不是 Git 仓库，尚无可推送的远端配置。
- 推送前必须确认目标 GitHub 仓库和发布范围。
- 不得提交 config.yaml 中的实际密钥、data/ 真实数据库、可执行文件、node_modules 或构建产物。
